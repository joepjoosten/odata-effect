---
"@odata-effect/odata-effect": patch
---

Include failed HTTP response status codes and response bodies on `ODataError` instances so request failures are easier to debug across V2, V4, and operation helpers.
